using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace NoviTests
{
    [TestClass]
    public class DependancyTest
    {
        [TestMethod]
        public void TestsRunning()
        {
            // If this test can run, return true
            Assert.IsTrue(true);
        }
    }
}
